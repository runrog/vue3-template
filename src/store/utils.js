/*
// TODO for most mutations / actions, just make global ones
// for arrays
setValue, (use $set fallback)
prependArray
appendArray
*/

// import * as types from '@/store/mutation-types';

// const mutations = {
//   [types.SET_USER](state, { user }) {
//     state.user = user;
//   },
// };
//
// const actions = {
//   setUser({ commit }, params) {
//     commit(types.SET_USER, params);
//   },
// };

export default {
  // mutations,
  // actions,
};
